# .env files should never be indexed by git.
# Howver, we include .envExample... files such that
# all our servers just work out of the box for development purposes.
.*env*
!.envExampleFlarum
!.envExampleDB
!.envExampleNginx

# Ignore all data files.
# FYI, when you run docker compose, all the data files will be written inside
# "data" folder. Docker creates bind mounts inside the "data" folder.
data/*
!data/README.md


