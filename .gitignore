# .env files should never be indexed by git.
# Howver, we include .exampleenv... files such that
# all our servers just work out of the box for development purposes.
.*env*
!exampleenvflarum
!exampleenvmariadb

# These PHP composer files are created the first time
# we run a development server because docker compose
# mount these files as docker volumes.
# We don't need these to be controlled via git,
# so we gitignore these.
composer.json
composer.lock
