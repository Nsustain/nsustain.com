# .env files should never be indexed by git. Instead,
# cp .envexample .env in order to use it.
.*env*
!.envexample

# Ignore all data files for Flarum, MariaDB, and Vault.
# FYI, when you run docker compose, all the data files will be written inside
# "data" folder. Docker creates bind mounts inside the "data" folder.
data/*
!data/README.md


